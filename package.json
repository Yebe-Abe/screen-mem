{
  "name": "screen-memory",
  "version": "0.1.0",
  "description": "A screen activity observer that records, understands, and indexes what you're doing for any LLM agent to browse.",
  "type": "module",
  "bin": {
    "screen-memory": "./bin/screen-memory.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
